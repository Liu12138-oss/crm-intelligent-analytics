## 1. 授权主体解析基础能力

- [x] 1.1 为 `Customer` 与 `Opportunity` 新增“负责人 + 协作人集合”解析能力，优先接入 CRM 官方详情 / 协作人接口，并补齐受控兜底入口说明
- [x] 1.2 扩展组织范围相关服务，支持按一组负责人 / 协作人 CRM 用户 ID 解析递归上级链、缺失映射和可审计关系摘要
- [x] 1.3 重构对象级授权服务，使其消费授权主体集合并移除“同直属小组成员可跟进”的旧放行逻辑

## 2. 企业微信跟进写回链路接线

- [x] 2.1 在主题型跟进、今日跟进和日报补名称等所有草稿创建入口接入新的授权主体集合判权，未通过时禁止创建草稿
- [x] 2.2 在最终确认写回和失败后重试阶段接入“最新负责人 + 最新协作人集合”重读与重新判权，关系失效时阻断执行并保留草稿
- [x] 2.3 更新企业微信跟进拒绝提示、失败原因和审计快照，明确区分动作权限不足、负责人关系不满足、协作人关系不满足和事实缺失

## 3. 契约与文档同步

- [x] 3.1 更新跟进写回相关共享类型、DTO、OpenAPI 与会话返回载荷，补齐协作人范围对象级授权所需字段或说明
- [x] 3.2 更新 `specs/001-crm-intelligent-analytics/spec.md`、`data-model.md`、`quickstart.md`，把“同直属小组成员可跟进”替换为“负责人 / 协作人及其递归上级链可跟进”
- [x] 3.3 更新 CRM API-first 相关设计说明，明确协作人读取优先复用官方接口，受控数据库兜底仅作为缺口场景

## 4. 测试与回归

- [x] 4.1 为授权主体解析与对象级授权补齐单元测试，覆盖负责人本人、协作人本人、负责人递归上级、协作人递归上级、同组非协作人拒绝和事实缺失阻断
- [x] 4.2 为企业微信跟进链路补齐集成测试，覆盖草稿创建放行 / 阻断、最终确认前协作人变更阻断和管理员放行场景
- [x] 4.3 回归现有企业微信跟进、失败重试、日报复用和权限拒绝审计断言，确保旧“同组成员允许”规则已被完全移除
