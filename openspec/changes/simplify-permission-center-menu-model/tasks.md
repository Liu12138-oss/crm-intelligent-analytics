## 1. 基线梳理与回归样本

- [x] 1.1 导出当前角色权限样本，覆盖系统管理员、经营管理者、普通业务问数角色、合同审核角色、企业微信机器人角色、审计角色和至少一个历史半配置角色。
- [x] 1.2 梳理当前 `visibleMenus`、`actionKeys`、`webConsoleEnabled`、`wecomBotEligible`、`exportAllowed`、合同审核扩展字段与真实页面/接口/企业微信工作流的对应关系。
- [x] 1.3 建立新旧权限映射验收表，逐项列出旧字段如何回显为新菜单包和风险子权限。
- [x] 1.4 标记历史 `webConsoleEnabled=true` 但没有任何 Web 菜单的异常角色，明确上线前需要管理员补充菜单或保持旧配置不被静默保存。
- [x] 1.5 确认本次不改变 CRM 数据范围、字段白名单、导出阈值、企业微信灰度、合同对象级权限和审计留痕边界。

## 2. 简化权限映射核心

- [x] 2.1 新增统一简化权限映射模块，提供旧运行时字段转菜单权限树的回显函数。
- [x] 2.2 新增统一简化权限映射模块，提供菜单权限树转旧运行时字段的保存函数。
- [x] 2.3 实现 `Web 入口` 派生规则：任意 Web 菜单存在时派生 `webConsoleEnabled=true`，无 Web 菜单时派生 `false`。
- [x] 2.4 实现风险子权限约束：导出、查询他人合同、下载他人合同或审核产物不得脱离所属主菜单保留。
- [x] 2.5 实现企业微信机器人权限包映射：勾选后派生 `wecomBotEligible=true` 和当前已上线 `wecom.*` 运行时动作，取消后清理相关派生动作。
- [x] 2.6 实现系统维护菜单映射：权限中心、查询模板管理、连接策略、AI配置、审计中心分别派生对应内部治理动作。
- [x] 2.7 为映射模块补充单元测试，覆盖旧数据回显、新配置保存、风险子权限清理、Web 入口派生、企业微信包派生和审计中心派生。

## 3. 后端接口与权限决策适配

- [x] 3.1 调整角色权限列表和详情接口，返回简化权限树字段，同时保留现有运行时字段用于兼容前端其它区域。
- [x] 3.2 调整角色权限保存接口，支持简化权限树提交格式，并统一通过映射模块生成旧运行时字段。
- [x] 3.3 确保权限预览接口同时返回菜单包、风险子权限和必要内部动作摘要，且复用正式权限决策服务。
- [x] 3.4 检查 `AccessDecisionService` 的权限合并逻辑，确保启用角色按新保存结果合并，停用角色不参与合并。
- [x] 3.5 检查 `PermissionEnforcementService` 拒绝审计，确保菜单包派生动作和风险子权限拒绝都能写入可排障审计。
- [x] 3.6 检查 `SessionCapabilitiesService` 能力快照，确保前端仍能拿到菜单、动作、导出资格、合同权限和企业微信入口状态。
- [x] 3.7 保留旧载荷兼容路径，确保回滚到旧权限抽屉时仍能读取和保存当前生成的旧字段。

## 4. 前端权限中心体验改造

- [x] 4.1 将 `RolePermissionFormDrawer.vue` 改为菜单权限树布局，移除普通管理员可见的基础开关区和动作权限大列表。
- [x] 4.2 在业务功能分组展示 `智能分析`、`经营报表`、`智能合同审核` 及其风险子权限。
- [x] 4.3 在移动端入口分组展示 `企业微信机器人` 单个入口包。
- [x] 4.4 在系统维护分组展示 `权限中心`、`查询模板管理`、`连接策略`、`AI配置`、`审计中心`。
- [x] 4.5 当主菜单取消勾选时，前端同步取消并禁用其风险子权限，避免提交不合法组合。
- [x] 4.6 对历史半配置角色显示明确中文提示，提醒管理员保存前必须确认至少一个 Web 菜单或取消 Web 入口能力。
- [x] 4.7 调整权限中心列表和预览展示文案，优先展示业务菜单包与风险子权限，不再展示重复的内部动作键。
- [x] 4.8 补充前端单元测试，覆盖抽屉回显、主子权限联动、保存载荷、历史半配置提示和权限预览展示。

## 5. 页面路由与业务动作回归

- [x] 5.1 调整前端路由守卫，使页面入口以 `requiredMenu` 为主，避免常规页面被隐藏动作重复阻断。
- [x] 5.2 保留导出、查询他人合同、下载他人合同或审核产物等风险动作的按钮可用态判断。
- [x] 5.3 回归智能分析：勾选 `智能分析` 后可进入页面、提交问数、查看结果详情、继续追问、查看常用查询和最近查询。
- [x] 5.4 回归智能分析导出：未勾选 `导出数据` 时前端不可导出且后端拒绝；勾选后仍受单次行数和每日次数限制。
- [x] 5.5 回归经营报表：勾选 `经营报表` 后可进入页面、查询报表、切换专题和查看详情。
- [x] 5.6 回归经营报表导出：未勾选 `导出数据` 时前后端均阻断；勾选后可导出且保留审计。
- [x] 5.7 回归智能合同审核：勾选 `智能合同审核` 后可查看有权访问的 CRM 合同列表并发起审核任务。
- [x] 5.8 回归合同跨查看和跨下载：未授予风险子权限时他人合同详情和产物下载均被阻断；授予后按当前 CRM 与合同审核边界放行。
- [x] 5.9 回归系统维护菜单：权限中心、查询模板管理、连接策略、AI配置、审计中心勾选后可执行对应页面常规维护能力。
- [x] 5.10 回归审计中心 reveal：勾选审计中心后 SQL 审计和敏感明细 reveal 可用，reveal 行为必须新增审计事件。

## 6. 企业微信机器人回归

- [x] 6.1 回归企业微信机器人权限包：勾选后具备企业微信入口资格，取消后企业微信入口被角色侧拒绝。
- [x] 6.2 回归企业微信灰度 `DISABLED`、`PILOT_ONLY`、`FULL` 三种模式，确认角色权限包不得绕过灰度。
- [x] 6.3 回归企业微信用户级停用名单，确认停用名单优先于角色包和灰度白名单。
- [x] 6.4 回归企业微信问数，确认具备机器人包且通过灰度的用户可正常问数，结果仍按 CRM 数据范围收口。
- [x] 6.5 回归企业微信新增客户和新增商机，确认仍需字段抽取、必填校验、候选确认和用户确认。
- [x] 6.6 回归企业微信跟进写回，确认仍需对象唯一命中、负责人/协作人/上级/管理员对象级授权、确认和幂等。
- [x] 6.7 回归企业微信日报预览，确认仍按当前 CRM 身份、负责人关系和日报治理策略计算范围。
- [x] 6.8 补充企业微信集成测试，覆盖机器人包放行、灰度拒绝、对象级拒绝和安全前置拒绝。

## 7. 文档、覆盖矩阵与验证

- [x] 7.1 更新 `docs/testing/feature-permission-enforcement-matrix.md`，从 36 个细粒度可见权限改为菜单包与风险子权限覆盖矩阵。
- [x] 7.2 更新根目录管理员权限使用说明，删除旧基础开关和动作大列表说明，改为新菜单树说明。
- [x] 7.3 更新 `README.md` 中权限中心相关说明，明确当前配置口径为菜单包与风险子权限。
- [x] 7.4 若主规格或快速开始文档引用旧权限项，检查并同步更新 `specs/001-crm-intelligent-analytics/spec.md`、`quickstart.md`、`tasks.md` 和 `contracts/openapi.yaml` 中相关描述。
- [x] 7.5 运行后端权限相关单元测试，至少覆盖 `feature-permission-catalog`、`access-decision`、`permission-enforcement`、`access-governance`、合同审核权限和企业微信权限测试。
- [x] 7.6 运行后端集成测试，至少覆盖 Web 分析、经营报表、查询模板、合同审核、审计中心、企业微信机器人和日报预览。
- [x] 7.7 运行前端单元测试，至少覆盖权限中心页面、角色权限抽屉、路由守卫、分析工作台、经营报表、合同审核和审计页面。
- [x] 7.8 使用真实浏览器或 Playwright 完成管理员端冒烟验证：编辑角色、保存权限、刷新会话、登录目标用户、访问各菜单和风险动作。
- [x] 7.9 输出上线前权限映射对比结果，确认关键角色没有因简化配置失去既有功能。
- [x] 7.10 记录回滚步骤，确认旧字段仍可支持回滚到原角色权限抽屉和原后端执行语义。
