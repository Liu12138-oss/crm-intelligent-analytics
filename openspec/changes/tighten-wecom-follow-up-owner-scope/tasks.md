## 1. 对象级授权基础能力

- [x] 1.1 新增 `FollowUpAuthorizationService`，统一输出负责人本人、直属小组成员、递归上级、管理员和拒绝原因等对象级授权结果
- [x] 1.2 在组织范围相关服务中补齐可复用的企业微信关系解析能力，包括 CRM 用户与 `wxUserid` 映射、直属小组成员解析和递归上级链解析
- [x] 1.3 为对象级授权补齐目标对象当前负责人读取能力，覆盖 `Customer` 与 `Opportunity` 两类对象
- [x] 1.4 调整待写回对象模型、工作记忆和共享类型，新增中性 `objectType/objectId/objectTitle` 语义并保留一版旧字段兼容读取

## 2. 企业微信跟进写回链路接线

- [x] 2.1 在主题型跟进、今日跟进和日报补名称等所有待写回草稿创建入口接入对象级授权校验，未通过时禁止创建草稿
- [x] 2.2 在最终确认写回和失败后重试写回阶段接入实时负责人重查与对象级授权重校验，关系变化时阻断执行并保留草稿
- [x] 2.3 统一更新企业微信跟进拒绝提示、失败原因和审计快照，能够区分动作权限不足与对象关系不满足
- [x] 2.4 统一整理 `Customer` / `Opportunity` 双对象写回返回载荷，确保客户跟进不再沿用 `Opportunity-only` 语义

## 3. 契约与文档同步

- [x] 3.1 更新 `backend` / `frontend` 跟进写回相关 DTO 与类型定义，正式支持 `Customer` 与 `Opportunity` 双对象枚举
- [x] 3.2 更新 `specs/001-crm-intelligent-analytics/contracts/openapi.yaml`，同步对象类型枚举、响应说明和相关契约字段
- [x] 3.3 更新 `specs/001-crm-intelligent-analytics/spec.md`、`data-model.md`、`quickstart.md`，补齐对象级授权边界、双对象写回口径和验证场景

## 4. 测试与回归

- [x] 4.1 为对象级授权服务补齐单元测试，覆盖负责人本人、同直属小组、递归上级、无关平级、映射缺失和上级链缺失场景
- [x] 4.2 为企业微信跟进链路补齐集成测试，覆盖 `Customer` / `Opportunity` 双对象的允许写回、草稿创建阻断和最终确认前关系失效阻断
- [x] 4.3 为权限拒绝审计补齐断言，验证对象关系拒绝与动作权限拒绝能被正确区分和追溯
- [x] 4.4 回归现有企业微信跟进、日报复用、群共享确认和失败重试测试，确保对象级授权接入后不破坏既有稳定链路
