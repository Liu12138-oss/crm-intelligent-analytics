## 1. AI 配置中心命名与数据模型

- [x] 1.1 将治理导航、路由标题、权限目录标签、权限中心展示文案和相关操作提示中的“AI 模型”统一改为“AI配置”，同时保持现有路由地址、菜单 key 和动作 key 不变
- [x] 1.2 在领域类型和应用存储模型中新增 `AiContextPolicyRecord`，并把记录挂入 `AppStorageState`
- [x] 1.3 在默认样例数据与本地持久化初始化逻辑中补齐 AI 上下文治理策略默认值，确保本地联调和历史空存储都可回退到可用初始值

## 2. 后端治理接口与策略服务

- [x] 2.1 新增 AI 上下文治理策略的 schema、仓储和服务，统一负责参数校验、默认值归一化和持久化读写
- [x] 2.2 在 `governance/ai-models` 路由前缀下新增上下文策略读取与更新接口，并继续复用 `ai_profile.manage` 作为访问动作
- [x] 2.3 扩展前端 `analysis.ts` 类型和 `analysis.service.ts`，补齐 AI 上下文治理策略的读取与保存请求模型
- [x] 2.4 为上下文策略保存与失败校验补齐中文错误提示，避免接口直接向页面暴露内部英文字段名

## 3. AI 配置中心页面整合

- [x] 3.1 改造 `AiModelProfilePage.vue`，在现有 AI Profile 摘要与列表之外新增“上下文策略”治理分区
- [x] 3.2 为 AI 上下文治理策略补齐独立的表单组件或页面分区交互，支持读取当前值、编辑、保存、成功反馈和失败反馈
- [x] 3.3 更新 `AppShell.vue`、`router/index.ts`、`business-code-labels.ts`、权限展示页及相关页面文案，确保用户看到的入口和标题统一为“AI配置”
- [x] 3.4 校正 AI 配置中心中的字段命名和帮助说明，统一使用“上一轮问题保留上限”“上一轮结果摘要保留上限”等中文业务文案

## 4. 运行时上下文策略生效

- [x] 4.1 新增统一 `AiContextPolicyService` 或等价运行时能力，集中提供文本裁剪、历史摘要压缩和失活时间判断方法
- [x] 4.2 改造 `WecomAiConversationOrchestrationService`，将会话轮次压缩、历史摘要长度、上一轮问题长度和上一轮结果摘要长度改为统一消费后台策略
- [x] 4.3 改造 `WecomBotService` 的活跃任务恢复逻辑，在日报、跟进、创建客户 / 商机场景下按任务态会话失活时长清理过期任务上下文
- [x] 4.4 改造 `AnalysisService` 的解释型追问与改条件追问复用逻辑，按普通分析会话失活时长、上一轮问题长度和上一轮结果摘要长度决定是否继续复用旧上下文
- [x] 4.5 为超时失效场景补齐统一处理口径，明确是转为新问题执行还是返回“上下文已失效”的引导提示

## 5. 审计、测试与文档

- [x] 5.1 扩展 AI 治理审计事件，覆盖上下文策略更新、失败保存及必要的策略摘要快照
- [x] 5.2 补充后端测试，覆盖上下文策略接口、默认值回退、长度裁剪、轮次压缩和普通分析 / 任务态失活判断
- [x] 5.3 补充前端测试，覆盖“AI配置”菜单命名、页面标题、上下文策略分区渲染和中文文案约束
- [x] 5.4 更新 `docs/architecture/ai-model-profile-governance.md`、`README.md`、`specs/001-crm-intelligent-analytics/quickstart.md` 等文档，补充 AI 配置中心命名、上下文策略项说明和生效验证步骤
