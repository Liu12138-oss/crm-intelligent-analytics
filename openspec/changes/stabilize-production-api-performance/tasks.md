## 1. 运行态控量与归档

- [x] 1.1 为 `SqlAuditRepository` 增加运行态 SQL 审计保留上限配置，默认仅保留最近 `SQL_AUDIT_RUNTIME_MAX_RECORDS` 条记录。
- [x] 1.2 编写只处理 `sqlAuditRecords` 的运行态归档脚本，支持备份、按时间或条数归档、执行后校验关键集合数量。
- [x] 1.3 为归档脚本补充测试，覆盖只裁剪 SQL 审计、不修改会话、模板、权限、企业微信上下文和合同审核状态。
- [x] 1.4 更新生产运维说明，记录备份、归档、恢复和敏感信息保护要求。

## 2. SQL 审计非阻塞写入

- [x] 2.1 抽象 SQL 审计写入接口，保留现有同步写入实现作为默认稳定路径。
- [x] 2.2 实现普通只读 SQL 审计异步队列，支持批量刷盘、队列长度阈值、flush 和降级日志。
- [x] 2.3 为阻断、写回、导出、reveal 和高风险失败审计保留高可靠写入路径。
- [x] 2.4 增加 `SQL_AUDIT_ASYNC_ENABLED`、队列阈值和批量刷盘间隔配置，并确保默认不开启异步路径。
- [x] 2.5 补充单元测试，验证普通只读审计不等待完整持久化、高风险审计可靠记录、队列超阈值时低风险审计降级。

## 3. SQL 审计独立存储

- [x] 3.1 设计并新增 `sql_audit_records` 或等价独立审计存储结构，包含时间、用户、模块、状态、请求 ID、会话 ID、SQL 指纹等索引。
- [x] 3.2 实现独立审计存储仓储，支持创建、分页列表、详情查询、统计聚合和 reveal 参数解析。
- [x] 3.3 实现运行态审计到独立存储的迁移脚本，支持批量写入、断点续跑和字段完整性校验。
- [x] 3.4 支持双写灰度和对账输出，记录两边写入数量、失败数量和关键字段差异。
- [x] 3.5 改造 SQL 审计控制器，独立存储启用时优先读取新存储，未启用时兼容读取旧运行态。
- [x] 3.6 补充仓储和控制器测试，覆盖新旧读取兼容、分页性能、详情、reveal 权限和新存储不可用降级。

## 4. 身份解析与权限快照稳定化

- [x] 4.1 梳理登录后页面初始化的身份解析调用链，记录当前角色、部门、组织和企业微信映射查询次数。
- [x] 4.2 调整会话用户短缓存 TTL 配置和并发去重逻辑，确保同一会话短时间内不重复执行等价身份查询。
- [x] 4.3 在权限策略、角色权限、数据范围和模板治理变更后主动失效能力快照和身份短缓存。
- [x] 4.4 评估并合并 `getUserById`、角色和部门查询，保持现有 `CrmUser` 输出字段兼容。
- [x] 4.5 补充回归测试，覆盖登录、会话恢复、权限变更后缓存失效、普通用户和管理员范围判断。

## 5. 性能观测与告警

- [x] 5.1 为后端关键接口增加请求总耗时、CRM Open API 耗时、CRM SQL 耗时、审计持久化耗时和身份连接池恢复日志。
- [x] 5.2 增加运行态文件大小、SQL 审计保留数量、审计队列长度、批量刷盘耗时和事件循环延迟采样。
- [x] 5.3 更新 Nginx 示例配置，增加 `$request_time`、`$upstream_response_time`、`$upstream_connect_time`、`$upstream_status` 和 `$request_id`。
- [x] 5.4 编写只读生产巡检脚本，输出接口性能、运行态体积、审计数量和队列指标，且不得输出明文密钥或密码。
- [x] 5.5 补充测试或脚本验证，确认日志和巡检输出不包含敏感配置值。

## 6. 回归与发布验证

- [x] 6.1 执行后端单元测试和契约测试，覆盖 SQL 审计、鉴权、治理审计、模板列表和会话恢复。
- [x] 6.2 执行前端单元和端到端测试，覆盖登录、分析工作台、治理审计、合同审核和页面初始化。
- [x] 6.3 使用膨胀运行态样本执行性能回归，验证登录、能力快照、模板列表、最近查询和历史查询不随审计数量线性变慢。
- [x] 6.4 在测试环境演练归档、异步审计开启/关闭、独立存储双写、迁移和回滚。
- [x] 6.5 更新 `docs/architecture/线上接口性能稳定治理方案.md` 和生产部署相关文档中的实施状态、参数和验收记录。
