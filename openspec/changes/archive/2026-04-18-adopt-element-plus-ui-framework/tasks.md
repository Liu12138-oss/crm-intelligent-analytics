## 1. 依赖接入与主题基础

- [x] 1.1 在 `frontend/package.json` 中添加 `element-plus` 和 `@element-plus/icons-vue` 运行时依赖，并更新锁文件
- [x] 1.2 在 `frontend/src/main.ts` 中按项目约定接入 Element Plus 样式和组件能力，确保不会影响现有 Pinia、router 和认证初始化顺序
- [x] 1.3 新增或整理 Element Plus 主题覆盖文件，将主色、状态色、圆角、边框、字体、焦点、禁用和加载样式映射到 `DESIGN.md` 设计令牌
- [x] 1.4 确认 Element Plus 样式导入后，登录页、应用壳层、表格和按钮不出现全局样式污染

## 2. 图标体系与项目封装

- [x] 2.1 新增项目级图标映射模块，集中导出导航、分析、合同、治理、审计、上传、下载、刷新、筛选、搜索、返回、关闭、展开收起、成功、失败、警告和风险图标
- [x] 2.2 将应用壳层导航从文字首字占位替换为 Element Plus 图标，并保留当前路由、激活态和收起逻辑
- [x] 2.3 为状态标签、风险提示、上传区、导出、刷新、筛选、搜索、返回、关闭、展开收起等关键操作补齐图标与中文说明
- [x] 2.4 建立基础图标使用规范，禁止页面自行使用无语义符号或临时字符作为关键图标

## 3. 基础组件替换

- [x] 3.1 将全局按钮语法迁移到 Element Plus 或项目封装按钮，保留所有请求型按钮的禁用、`aria-busy`、加载文案和 spinner 反馈
- [x] 3.2 将输入框、密码框、文本域、选择器和筛选控件迁移到 Element Plus 或项目封装组件，保留现有 `v-model` 与占位文案
- [x] 3.3 将标签、提示块、空状态、加载状态、失败状态、风险状态和消息反馈迁移到 Element Plus 或项目封装组件，并确保状态不只依赖颜色
- [x] 3.4 将表格容器迁移到 Element Plus 表格能力或兼容封装，保留横向滚动、空状态、导出入口和字段展示口径

## 4. 页面分批重构

- [x] 4.1 重构 `LoginPage.vue`，用 Element Plus 表单、输入框、按钮、提示和图标增强账号密码登录与企业微信扫码登录，保留现有登录逻辑、文案和管理员信息
- [x] 4.2 重构 `AppShell.vue`，用 Element Plus 图标和导航语义增强左侧导航、顶部栏、退出登录和右侧信息区，保留路由目标、`isActive`、`hintText` 和 `logout` 调用
- [x] 4.3 重构分析工作台与分析组件，包括问数输入、常用查询、最近查询、结果摘要、指标、图表、表格、导出、详情抽屉和请求反馈
- [x] 4.4 重构分析结果详情页，覆盖筛选条件、权限范围、可信说明、执行快照、推荐追问、导出、追问和结果明细
- [x] 4.5 重构合同审核工作台与详情页，覆盖上传、拖拽、最近任务、总体结论、一票否决、高风险项、审核依据、降级提示和产物下载状态
- [x] 4.6 重构治理策略、模板管理和连接策略页面，覆盖表单、阈值摘要、状态提示、创建/保存请求态和图标语义
- [x] 4.7 重构审计中心页面，覆盖审计分类、摘要卡、筛选区、事件表格、AI 预警、治理建议和刷新/筛选请求态

## 5. 行为保持与回归测试

- [x] 5.1 核对本次重构未修改后端 API、数据库、OpenAPI 契约、企业微信能力、权限模型、service 方法签名和 router 路径
- [x] 5.2 核对登录、退出、发起分析、刷新状态、常用查询、最近查询、导出、查看详情、提交追问、上传合同、下载产物、保存策略、创建模板、保存连接策略、刷新审计和筛选审计事件绑定保持原有调用
- [x] 5.3 补充或更新单元测试，覆盖 Element Plus 组件接入后的登录请求态、导航图标、分析请求态、合同上传、治理保存和审计筛选
- [x] 5.4 补充或更新 E2E 测试，覆盖登录页、分析工作台、结果详情、合同审核、治理策略、模板管理、连接策略和审计中心主链路
- [x] 5.5 人工核对桌面、窄屏和移动端下 Element Plus 组件不会造成横向溢出、标签换行、按钮拥挤或表格不可读

## 6. 验证与交付

- [x] 6.1 运行 `openspec validate adopt-element-plus-ui-framework --strict`
- [x] 6.2 运行 `pnpm --dir frontend lint`
- [x] 6.3 运行 `pnpm --dir frontend test:unit`
- [x] 6.4 运行 `pnpm --dir frontend test:e2e`
- [x] 6.5 运行 `pnpm --dir frontend build`
- [x] 6.6 更新实施记录，说明 Element Plus 组件覆盖范围、图标补齐范围、未迁移页面和后续风险
