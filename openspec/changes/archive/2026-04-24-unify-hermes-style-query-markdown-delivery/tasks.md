## 1. 入口双轨与语义识别收敛

- [x] 1.1 盘点企业微信与 Web 入口中的固定功能主题入口，明确哪些输入继续走固定程序流、哪些输入进入自由问数主链
- [x] 1.2 调整统一 AI 入口理解 contract，让自由问数输出最小受控语义抽取结果，而不是强依赖窄枚举问数意图
- [x] 1.3 修改企业微信与 Web 入口编排逻辑，确保固定功能优先匹配，自由问数只在未命中固定功能时进入问数后端
- [x] 1.4 为入口理解补齐 `usedFallback`、`fallbackReason`、固定功能 / 自由问数分流结果的审计快照

## 2. 统一问数知识层

- [x] 2.1 设计问数知识层的数据结构，覆盖字段别名、业务口径别名、组织别名、码表翻译、高频模板和已验证查询示例
- [x] 2.2 将现有库表梳理、业务口径说明和 Hermes 可复用语义资产整理到统一问数知识层入口
- [x] 2.3 为高频问数模板建立可治理资产，支持模板优先命中与查询任务生成复用
- [x] 2.4 为问数知识层补齐回归样例，覆盖口语化表达、中英混写、错别字和业务简称

## 3. Hermes 化受控直查主链

- [x] 3.1 将自由自然语言问数默认执行模式调整为 `GUARDED_DIRECT_QUERY`，并保留 `PLAN_EXECUTION` 作为复杂兜底
- [x] 3.2 改造受控查询任务生成器，使其能基于最小受控语义和知识层输出 1 到 3 个受控查询任务
- [x] 3.3 保持现有 API-first、白名单、AST、预检、权限注入和审计安全栈不变，让受控查询任务继续命中过滤链路
- [x] 3.4 调整解释型追问与改条件追问分流，让解释复用继续消费最近一次结果包，改条件追问重新进入受控直查主链

## 4. 统一结果包与 Markdown 交付

- [x] 4.1 在统一结果包中增加完整 Markdown、企业微信裁剪版 Markdown 和章节大纲等展示载荷
- [x] 4.2 改造 grounded 洞察生成逻辑，让 AI 基于结果集、权限摘要和执行快照生成 Markdown 总结
- [x] 4.3 增加 Markdown 与结构化结果的一致性校验，阻止事实冲突的结果正式交付
- [x] 4.4 调整渠道呈现层，让 Web 与企业微信消费同一份总结源，只做展示裁剪不改业务口径

## 5. Web 预览与企业微信消息适配

- [x] 5.1 为前端引入受控 Markdown 渲染方案，并实现安全清洗与受限语法渲染
- [x] 5.2 在 Web 智能分析工作台或结果详情页增加 Markdown 预览区域，同时保留结构化明细、重跑、导出和审计入口
- [x] 5.3 为企业微信问数结果实现 Markdown 子集输出、超长裁剪和必要的“到 Web 查看完整结果”提示
- [x] 5.4 调整最近查询、结果详情和导出复用逻辑，确保它们继续基于统一结果包工作，而不是直接消费 Markdown 文本

## 6. 测试、灰度与迁移

- [x] 6.1 补齐固定功能 / 自由问数分流、自由表达识别和知识层命中的单测与集成测试
- [x] 6.2 补齐受控直查主链、计划执行兜底、解释复用和改条件追问的回归测试
- [x] 6.3 补齐 Markdown 生成、一致性校验、Web 安全渲染和企业微信裁剪交付的测试
- [x] 6.4 增加执行模式切换、Markdown 交付和知识层启用的灰度开关与回滚说明
