## 1. 语义资产治理模型与接口

- [x] 1.1 在应用存储模型、领域类型和默认运行时数据中新增语义资产记录、发布快照记录和最小资产分类枚举
- [x] 1.2 在 `governance` 域下新增语义资产的 schema、repository、service 和 controller，支持资产列表、详情、编辑、启停和发布
- [x] 1.3 为语义资产治理入口补齐中文文案、状态字段和最近发布摘要，确保后台不直接暴露开发内部字段名
- [x] 1.4 为语义资产新增、修改、启停、发布和回退补齐审计事件与审计快照字段

## 2. 语义资产接入受控分析主链

- [x] 2.1 改造 `AnalysisQueryKnowledgeService`，改为“已发布语义资产优先、静态常量兜底”的知识层装载模式
- [x] 2.2 将别名提示、时间口径提示、组织归一提示、已验证问法和负例样例统一接入自由问数知识上下文生成逻辑
- [x] 2.3 在知识缺失、资产停用或资产命中负例时，收口到补问、计划执行兜底或显式阻断的受控路径
- [x] 2.4 为知识层装载、资产命中和知识缺失 fallback 补齐后端回归测试

## 3. 统一执行轨迹摘要与审计可解释性

- [x] 3.1 在分析领域模型与结果 contract 中新增统一 `executionTraceSummary` 或等价执行轨迹摘要结构
- [x] 3.2 改造 `AnalysisWorkflowOrchestrator`，将语义资产命中、任务级 `resultKind`、执行来源、适配器、fallback 原因和一致性标识归并为统一轨迹摘要
- [x] 3.3 改造 `AnalysisService`、结果详情接口和审计详情载荷，让结果页与审计端消费同一份轨迹摘要而不是各自拼快照
- [x] 3.4 扩展审计聚合与排障视图，补充语义资产命中、执行来源和 fallback 阶段的聚合统计
- [x] 3.5 为统一轨迹摘要的生成、透传和失败阻断补齐测试

## 4. 统一结果协议与前端展示收敛

- [x] 4.1 扩展智能分析结果 section contract，补齐 `sourceNotes`、`footnotes` 和执行依据摘要字段
- [x] 4.2 改造 `AnalysisReportComposerService` 与相关结果类型，使智能分析结果可映射到共享区块协议
- [x] 4.3 在前端分析结果页与详情页接入共享区块渲染能力，优先复用经营报表已有的区块组件和口径说明浮层
- [x] 4.4 为 Web 结果详情新增执行依据摘要入口，并让区块级来源说明与缺失 / 降级说明可见
- [x] 4.5 校验企业微信裁剪结果、Markdown 交付、最近查询与导出仍继续消费同一份统一结果包

## 5. 文档、契约与回归同步

- [x] 5.1 更新 OpenAPI、前端类型和服务层请求模型，补齐语义资产治理接口与执行轨迹 / 结果协议增量字段
- [x] 5.2 更新 `specs/001-crm-intelligent-analytics/` 下相关主规格、数据模型、契约和测试清单，补充语义资产、执行轨迹和统一结果协议口径
- [x] 5.3 更新 `docs/architecture/` 与 `README.md`，说明语义资产治理、执行轨迹查看和报告协议统一后的协作入口
- [x] 5.4 补齐治理后台、分析结果页和审计页的前后端回归测试，并验证关闭治理资产读取时可安全回退到静态知识层
