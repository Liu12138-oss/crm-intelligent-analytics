## 1. 统一入口契约

- [x] 1.1 定义统一 `AiEntryIntent` schema、场景枚举、结构化槽位约束与校验器，覆盖 Web 首次问数、Web 追问、企业微信空闲态消息、企业微信活跃任务回复、日报 / 跟进自由文本和创建链路续填。
- [x] 1.2 扩展统一审计快照模型，明确记录 `language`、`usedFallback`、`fallbackReason`、`targetWorkflow`、最终固定程序和执行门闩结果。
- [x] 1.3 收紧 AI 入口相关特性开关语义，确保关闭 AI 主链时只退回帮助、澄清或阻断，不恢复旧规则主链。

## 2. Web 分析收敛

- [x] 2.1 将 Web 首次问数统一改为先消费 `AiEntryIntent` 结果，再决定进入计划执行、受控直查、补问或阻断。
- [x] 2.2 将解释型追问与改条件追问统一改为先消费 `AiEntryIntent` 结果，再决定结果复用解释或重新取数。
- [x] 2.3 下沉本地中文语义规则为最小安全 fallback，仅保留安全阻断与澄清，不再承担主理解职责。

## 3. 企业微信消息收敛

- [x] 3.1 将企业微信空闲态消息路由统一改为先消费 `AiEntryIntent`，覆盖帮助、日报、跟进、创建、项目查询和分析问数。
- [x] 3.2 将企业微信活跃任务短回复统一改为先消费 `AiEntryIntent`，覆盖确认、取消、修改、继续和任务切换。
- [x] 3.3 保留并显式实现确定性执行门闩，确保写回、提交、创建和状态推进只能在上下文完整且权限合法时触发。

## 4. 自由文本抽槽与创建链路

- [x] 4.1 将跟进 / 日报自由文本整理改为 AI 抽槽位主链，旧 helper 下沉为 fallback。
- [x] 4.2 将新增客户链路的入口识别、字段续填和确认语义改为 AI 主链识别，旧入口关键词和确认词表下沉为 fallback。
- [x] 4.3 将新增商机链路的入口识别、字段续填、对象澄清和确认语义改为 AI 主链识别，旧正则和确认词表下沉为 fallback。

## 5. 团队日报与帮助能力收敛

- [x] 5.1 将个人日报查看与团队日报预览请求统一改为先消费 `AiEntryIntent`，不再由固定关键词组合直接承担主入口识别。
- [x] 5.2 将 AI 不可用时的企业微信帮助、澄清和阻断提示统一收口，避免不同入口各自维护一套规则主链。

## 6. 验证与灰度

- [x] 6.1 补齐按入口和场景拆分的集成测试，覆盖英文、中英混写、韩文 / 日文、错别字、口语化中文、歧义确认句和跨任务文本。
- [x] 6.2 补齐 AI 主链命中、fallback 命中、低置信澄清、非法结构阻断和高风险执行门闩的审计断言。
- [x] 6.3 输出灰度与回滚说明，明确观察指标、fallback 比例阈值和回退时仅保留最小安全 fallback 的策略。
