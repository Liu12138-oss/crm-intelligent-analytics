## 1. 存储模型与领域对象

- [X] 1.1 在应用库存储模型与 `domain.ts` 中新增 `RolePermissionRecord`、`WecomPilotPolicyRecord`、权限预览结果和身份映射诊断结果类型
- [X] 1.2 为角色权限矩阵和企业微信灰度策略新增 repository / state 读写能力，并补默认初始化逻辑
- [X] 1.3 增加兼容迁移逻辑：从现有 `enabledRoleIds`、`exportRoleIds` 和合同审核环境变量推导初始角色权限默认值
- [X] 1.4 为新增权限治理事件扩展审计事件类型、快照字段与基础映射结构

## 2. 权限治理后端接口

- [X] 2.1 实现角色权限矩阵查询与更新接口，支持菜单、动作、渠道、合同审核权限和变更说明维护
- [X] 2.2 实现企业微信灰度策略查询与更新接口，支持 `DISABLED / PILOT_ONLY / FULL`、白名单和停用名单维护
- [X] 2.3 实现权限中心顶部摘要接口，返回角色开通数、灰度状态、白名单人数、导出开通数和映射异常数
- [X] 2.4 实现用户级有效权限预览接口，支持按 `crmUserId` 与 `wecomUserId` 预览最终权限结果
- [X] 2.5 实现身份映射诊断接口，返回企业微信映射链、CRM 角色/部门、入口状态和失败原因

## 3. 统一权限决策与能力快照

- [X] 3.1 实现统一权限决策服务，串联 CRM 权限、角色能力矩阵、通道资格和企业微信灰度规则
- [X] 3.2 扩展 `GET /analysis/capabilities`，返回 `visibleMenus`、`actionKeys`、`wecomBotAccessState`、`wecomBotAccessReason` 和合同审核权限摘要
- [X] 3.3 将模板可见性、导出资格和全局字段/导出阈值叠加到统一权限决策中，避免业务模块各自重复判断
- [X] 3.4 为权限预览、灰度预览和关键拒绝结果写入统一审计事件

## 4. 企业微信入口灰度接入

- [X] 4.1 改造 `WecomAuthService`，在身份映射成功后、正式业务编排前接入企业微信灰度准入判断
- [X] 4.2 为企业微信入口统一输出 `ALLOWED`、`CHANNEL_DISABLED`、`ROLE_NOT_ENABLED`、`PILOT_REQUIRED`、`EXPLICITLY_DENIED`、`UNMAPPED_CRM_IDENTITY` 等状态码和中文提示
- [X] 4.3 让企业微信分析、受控新增、跟进写回和日报预览统一复用企业微信入口准入门闩，而不是各自维护独立灰度判断
- [X] 4.4 为企业微信灰度策略变更、灰度预览和准入拒绝补齐后端单测与集成测试

## 5. 合同审核权限整合

- [X] 5.1 将合同审核上传、跨任务查看和跨任务下载改为消费统一角色动作矩阵
- [X] 5.2 保留迁移期环境变量兜底，并实现“新配置优先、旧配置回退”的兼容逻辑
- [X] 5.3 确保合同审核详情和产物下载继续执行实时二次鉴权，权限撤销后立即生效
- [X] 5.4 为合同审核跨任务查看/下载授权与拒绝场景补齐单测和集成测试

## 6. 前端权限中心与消费改造

- [X] 6.1 在治理区新增 `权限中心` 页面，并落地顶部摘要、角色权限、企业微信灰度、数据与导出基线、身份映射诊断和变更记录分区
- [X] 6.2 实现角色权限编辑抽屉、企业微信灰度编辑面板和用户权限预览面板
- [X] 6.3 更新应用壳层与相关页面消费新的能力快照，按 `visibleMenus` 与 `actionKeys` 控制导航显隐和动作禁用
- [X] 6.4 在身份映射诊断页展示企业微信账号到 CRM 用户的完整命中链路和失败原因

## 7. 契约、文档与验收

- [X] 7.1 更新 OpenAPI / 前后端类型契约，补充权限中心、灰度策略、预览和诊断接口字段
- [X] 7.2 更新 `specs/001-crm-intelligent-analytics/quickstart.md`、相关验证场景与 README/治理说明，补充企业微信首批试点开通与回滚步骤
- [X] 7.3 补充权限中心、企业微信灰度、身份映射诊断、合同审核扩展授权和 Web 菜单显隐的端到端验收用例
- [X] 7.4 完成一次基于真实角色与测试账号的灰度演练，验证“Web 可用但企业微信未开通”“企业微信命中白名单”“单用户紧急停用”“权限撤销立即生效”四类关键场景
