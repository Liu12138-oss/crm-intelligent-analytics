## 1. 时间槽 Contract 与入口理解

- [x] 1.1 定义 `TemporalSlot`、`ResultTemporalScope` 等共享类型，并明确 `startAt/endAt`、时区、粒度、闭开区间和置信度字段。
- [x] 1.2 扩展 `analysis-intent-pack` 输出 schema，要求自由问数返回 `temporalSlot`，并保留 `packCode`、`packVersion` 与校验失败原因。
- [x] 1.3 调整 `analysis-intent-pack` 归一化逻辑，禁止在 AI 主链成功时用本地时间词表覆盖 AI 时间槽。
- [x] 1.4 将 `time-range.util` 从自由问数主执行链路移除或降级为测试辅助 / 迁移兼容工具，并显式记录 fallback 来源。
- [x] 1.5 为低置信、缺边界、歧义时间表达补充澄清提示和阻断结果，不再默认全量查询历史数据。

## 2. 受控查询任务生成与执行消费

- [x] 2.1 将 `TemporalSlot` 注入 `AnalysisWorkflowPlan`、受控直查任务上下文和执行快照。
- [x] 2.2 改造 `GUARDED_DIRECT_QUERY` 任务生成器，使 AI 基于时间槽生成 1 到 3 个受控查询任务。
- [x] 2.3 改造 `PLAN_EXECUTION` 编译链路，使查询计划使用同一时间槽生成参数化时间条件。
- [x] 2.4 增加对象到时间字段白名单，例如商机新增默认 `opportunities.created_at`，合同签约默认 `contracts.sign_date`。
- [x] 2.5 在执行前校验时间字段、时间边界、查询窗口上限、时区和相对时间基准，失败时阻断或补问。
- [x] 2.6 确保权限注入、API-first 路由、AST 校验、只读校验、预检、行数限制和超时限制继续覆盖带时间槽的查询任务。

## 3. 结果包、Markdown 与追问复用

- [x] 3.1 在统一结果包中增加 `temporalScope`，记录实际执行使用的时间口径。
- [x] 3.2 调整 Markdown 生成、企业微信裁剪、Web 预览、图表标题和明细表说明，统一展示 `temporalScope`。
- [x] 3.3 调整最近查询保存与重跑逻辑，保存时间槽来源，并按产品定义决定重跑时复用边界或重新计算相对时间。
- [x] 3.4 调整解释型追问和改条件追问，解释复用已有 `temporalScope`，改条件追问生成新的时间槽和新结果包。
- [x] 3.5 扩展结果一致性校验，阻止摘要、图表、表格、Markdown 或导出引用不同时间口径的结果。

## 4. 问数知识层与 Hermes 参考资产

- [x] 4.1 将 Hermes 项目中“对象、过滤条件、输出字段、模板优先、只读工具边界”的时间处理原则整理进当前问数知识层。
- [x] 4.2 为常见业务对象补充默认时间字段、可选时间字段和禁止时间字段说明。
- [x] 4.3 为高频时间问法补充示例和反例，不把示例落成本地 if/else 规则。
- [x] 4.4 为受控直查提示词补充时间槽使用要求，要求 AI 说明所选时间字段和时间边界。

## 5. 测试、灰度与回滚

- [x] 5.1 增加 AI 时间槽归一化单测，覆盖“最近四个月、前四个月、近 6 月、过去 12 个月、上季度、去年同期、2026 年一季度、本财年”。
- [x] 5.2 增加低置信时间表达测试，验证系统补问或阻断，不执行全量历史查询。
- [x] 5.3 增加 `PLAN_EXECUTION` 与 `GUARDED_DIRECT_QUERY` 共用同一时间槽的集成测试。
- [x] 5.4 增加结果包 `temporalScope` 与 Markdown、图表、明细一致性测试。
- [x] 5.5 增加最近查询重跑和改条件追问的时间槽回归测试。
- [x] 5.6 更新灰度和回滚说明，明确不得通过恢复本地时间词表主链作为长期回滚方案。
