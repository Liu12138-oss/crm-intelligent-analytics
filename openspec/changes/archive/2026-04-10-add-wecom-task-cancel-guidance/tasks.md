## 1. 统一能力目录与取消文案

- [x] 1.1 在 `backend/src/modules/wecom/wecom-ai-prompt.config.ts` 中新增统一的企业微信机器人“当前已支持功能”目录与取消收口提示构造函数
- [x] 1.2 梳理并统一取消关键词，覆盖“取消”“不做了”“先停一下”“不跟进了”等任务结束表达，同时避免误伤普通问数语句
- [x] 1.3 梳理并统一已支持任务入口识别口径，便于在活跃任务中判断“这是旧任务补充”还是“切换到新任务”

## 2. 会话层取消收口与任务切换实现

- [x] 2.1 在 `backend/src/modules/wecom/wecom-bot.service.ts` 中实现活跃任务识别、统一取消分支和新任务切换分支，覆盖跟进模板、跟进写回/群共享、客户创建、商机创建和日报确认场景
- [x] 2.2 复用 `backend/src/modules/wecom/wecom-ai-conversation-orchestration.service.ts` 中现有的记忆清理方法，封装统一的“清理当前活跃任务状态”逻辑
- [x] 2.3 对已有待写回记录、创建草稿和取消审计链路做兼容处理，确保取消后仍沿用原有状态落库与审计要求
- [x] 2.4 为任务切换补充“旧任务结束 + 新任务首轮提示”的过渡输出，避免把新任务入口误写入旧任务草稿

## 3. 验证与文档同步

- [x] 3.1 在企业微信相关集成测试中补充取消收口与任务切换场景，至少覆盖跟进模板中切到日报、创建任务中切到跟进，以及各任务取消后的能力引导输出
- [x] 3.2 为现有稳定链路补充“不应误切换”的回归测试，至少覆盖跟进模板正常续填、跟进写回确认继续修改、客户/商机创建字段补齐和日报确认续填等场景
- [x] 3.3 更新 `README.md` 中的“企业微信机器人当前支持能力”清单，列出当前稳定支持的用户可见能力
- [x] 3.4 更新 `AGENTS.md` 协作规约，要求后续新增、下线或调整企业微信机器人用户可见能力时，必须同步更新统一能力目录与主文档能力清单
