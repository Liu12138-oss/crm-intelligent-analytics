## 1. 类型与迁移准备

- [x] 1.1 扩展查询模板领域类型，新增范围治理快照、范围分类、风险项、展示口径来源、静态维度来源和审核状态字段
- [x] 1.2 扩展应用存储默认状态与兼容迁移逻辑，确保旧模板缺少治理字段时可读取并标记为待校验
- [x] 1.3 为查询模板仓储补齐治理字段归一化，避免历史 `.runtime/app-storage.json` 中的旧模板被静默当作安全模板
- [x] 1.4 新增模板范围治理快照构造工具，统一输出中文风险级别、风险说明和修复建议
- [x] 1.5 更新前后端共享类型，补齐治理后台需要展示的范围分类、审核状态、风险项和修复建议
- [x] 1.6 增加 `QUERY_TEMPLATE_SCOPE_GOVERNANCE_MODE=observe|enforce` 灰度配置，默认观察模式，强制模式由测试或发布配置显式开启
- [x] 1.7 更新分析范围快照结构，记录默认部门裁剪前后负责人数量与裁剪说明

## 2. SQL 范围分析与分类

- [x] 2.1 扩展 `QueryTemplateScopeAnalyzerService`，识别主业务表、表别名、组织字段、部门字段和负责人字段
- [x] 2.1a 增加 SQL 表达式血缘追踪，覆盖 `CASE`、函数、`GROUP BY`、CTE 输出列和子查询输出列到基础字段
- [x] 2.2 增加可自动收口模板识别，覆盖单主表 `opportunities`、`contracts`、`customers`、`users` 的常见 SELECT 查询
- [x] 2.3 增加固定范围识别，解析硬编码部门 ID、负责人 ID、部门名称和团队名称
- [x] 2.4 增加静态维度识别，检测 `UNION ALL` 团队清单、硬编码目标表和静态部门枚举
- [x] 2.5 增加复杂模板识别，检测多主表聚合、复杂 CTE、子查询多处可收口入口和无法确定权限主表的 SQL
- [x] 2.6 增加展示维度来源分析，识别 `team_name`、`department_name`、`owner_name`、图表维度和表格列来源
- [x] 2.7 增加展示口径一致性校验，阻断“按主业务对象过滤、按客户部门展示”等未审核风险
- [x] 2.8 为 SQL 解析失败场景输出 `COMPLEX_REVIEW_REQUIRED` 或 `UNSAFE_SCOPE`，不得默认放行

## 3. 自动注入与执行收口

- [x] 3.1 扩展 `QueryTemplateScopeInjectorService`，支持基于治理快照选择权限主表和别名注入范围条件
- [x] 3.2 调整 `QueryTemplateExecutionService.prepareTemplateQuery`，先生成治理快照，再决定自动注入、固定范围兼容校验、审核放行或阻断
- [x] 3.3 对 `AUTO_SCOPABLE` 模板执行自动范围注入，模板 SQL 不需要出现内部范围参数
- [x] 3.4 对 `DECLARED_DYNAMIC_SCOPE` 模板保留现有动态范围参数兼容校验，并补充治理快照记录
- [x] 3.5 对 `FIXED_SCOPE` 模板执行当前用户范围覆盖校验，未覆盖时返回中文无权限提示
- [x] 3.6 对未审核的 `COMPLEX_REVIEW_REQUIRED` 和 `UNSAFE_SCOPE` 模板阻断普通用户执行
- [x] 3.6a 观察模式下记录风险但兼容放行，强制模式下执行阻断
- [x] 3.7 将原始 SQL、治理快照、最终执行 SQL、参数和范围快照写入分析请求与 SQL 审计上下文
- [x] 3.8 确保模板预览、正式执行和最近查询重跑都走同一执行收口逻辑

## 4. 结果展示与导出边界

- [x] 4.1 扩展结果组装流程，让 `QueryResultPresentationService` 消费治理快照并保留展示口径说明
- [x] 4.2 对表格、图表 series、转置表和矩阵表应用同一展示口径校验结果
- [x] 4.3 在结果包 `scopeSummary` 或执行轨迹中补充模板范围说明，用户可看到“按当前权限范围展示”或“客户归属部门口径已审核”
- [x] 4.4 调整导出链路，模板导出补查必须复用当前治理快照和当前用户范围，不得导出页面外范围
- [x] 4.4a 历史模板结果导出前重新计算当前治理结论，强制模式不可执行时阻断并提示重跑
- [x] 4.5 调整最近查询记录，保存模板治理摘要但重跑时必须重新计算，不得沿用历史放行结论

## 5. 治理后台交互

- [x] 5.1 扩展查询模板校验接口，返回范围分类、风险项、修复建议、审核要求和可执行状态
- [x] 5.2 扩展查询模板保存接口，保存权限主表、权限主表别名、展示部门口径、目标表收口策略和审核结论
- [x] 5.3 更新查询模板治理页面，在 SQL 校验区展示中文风险提示和推荐修复方式
- [x] 5.4 增加复杂模板审核表单，支持治理人员声明权限主表、展示口径和静态目标收口策略
- [x] 5.5 增加模板预览反馈，展示当前模拟用户范围、执行范围分类和是否可对普通用户发布
- [x] 5.6 对未通过范围治理的模板禁用发布或执行按钮，并提供可操作的中文原因

## 6. 历史模板迁移

- [x] 6.1 编写历史模板分类脚本或一次性检查命令，输出全部模板的范围分类和风险列表
- [x] 6.2 迁移 `tpl_company_quarterly_opportunity_health`，确保新增商机月度分布按同一主业务归属口径过滤和展示
- [x] 6.3 迁移 `tpl_company_2026_completion`，将静态团队目标清单改为可按当前范围收口的受控目标口径或审核元数据
- [x] 6.4 检查客户维度提单、承诺商机季度拆分、近一周新增商机明细等高频模板是否存在展示口径与过滤口径不一致
- [x] 6.5 生成迁移记录，列出已自动放行、需审核、已阻断和已修复的模板清单

## 7. 自动化测试

- [x] 7.1 为范围分析器新增单元测试，覆盖自动收口、动态范围、固定范围、静态团队清单、复杂 CTE 和解析失败场景
- [x] 7.2 为展示口径一致性新增单元测试，覆盖按商机部门过滤却按客户部门展示的风险识别
- [x] 7.3 为模板执行新增集成测试，验证部门负责人执行默认全量模板时只返回授权范围
- [x] 7.4 为固定范围模板新增集成测试，验证有权限用户可执行、无权限用户被阻断
- [x] 7.5 为复杂模板新增集成测试，验证未审核静态目标模板阻断普通用户、审核后按声明口径执行
- [x] 7.6 增加牛劲/山东区回归样例，构造山东区 22 人范围与过宽直属链，确保结果不展示其它区
- [x] 7.7 为最近查询重跑新增测试，验证历史模板结果不会绕过当前范围治理
- [x] 7.8 为导出补查新增测试，验证导出范围与页面展示范围一致
- [x] 7.9 为治理后台新增前端测试，覆盖校验风险展示、审核表单和阻断按钮状态
- [x] 7.10 增加旧功能回归矩阵测试或检查清单，覆盖模板列表权限、普通模板执行、自由 AI 问数、最近查询展示、最近查询重跑、导出阈值、治理创建编辑和管理员全量视角

## 8. 文档与上线验证

- [x] 8.1 更新 `README.md` 或治理说明，明确模板作者可自由写业务 SQL，但执行必须统一受控收口
- [x] 8.2 更新 `specs/001-crm-intelligent-analytics/quickstart.md`，增加部门负责人执行全量模板只返回授权范围的验证场景
- [x] 8.3 更新 `docs/需求文档/企业微信目录同步运维说明.md` 或相关治理文档，补充组织范围过宽时的排查步骤
- [x] 8.4 在本地测试环境执行历史模板分类，确认高风险模板已识别为需审核或已收口
- [x] 8.5 使用牛劲同类账号回归样例复测新增商机月度分布和各团队完成预测，确认只展示授权范围
- [x] 8.6 使用管理员账号相关回归复测同一模板，确认仍可查看全量并保留全量审计说明
- [x] 8.7 补充发布前备份生产 `.runtime/app-storage.json`、发布后清理会话能力快照和需要重跑最近查询缓存的上线检查项；本次未直接操作生产环境
- [x] 8.8 通过 SQL 审计和分析请求审计自动化回归确认治理快照、范围快照和执行 SQL 可追溯
