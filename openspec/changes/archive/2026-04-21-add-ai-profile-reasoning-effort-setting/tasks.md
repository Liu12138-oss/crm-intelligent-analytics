## 1. 治理字段与默认值收口

- [x] 1.1 统一梳理 `AiModelProfile`、治理写入 schema、环境引导配置与运行时解析中的 `reasoningEffort` 缺省口径，明确空值统一落到 `minimal`
- [x] 1.2 调整 `AiModelProfileService` 的新建、更新、复制逻辑，确保未显式填写时默认写入最低推理等级
- [x] 1.3 调整 `AiModelEnvBootstrapService` 与环境兜底解析逻辑，确保环境默认档案和无激活 Profile 场景缺省使用最低推理等级

## 2. 前端治理表单与展示

- [x] 2.1 在 `AiProfileFormDrawer.vue` 新增“推理等级”选择项，并为新建与历史空值记录默认回显最低等级
- [x] 2.2 补齐治理前端类型、保存 payload 与测试 payload，确保推理等级可随表单提交到后端
- [x] 2.3 在治理列表、摘要卡或等价展示区补充当前推理等级显示，避免字段只能编辑不能查看

## 3. Provider 消费与行为边界

- [x] 3.1 核对 Codex 当前真实调用路径，确保激活 Profile 与环境兜底场景都会按保存后的推理等级透传
- [x] 3.2 核对 Claude SDK 当前是否支持显式推理等级参数，并按核对结果实现“真实透传”或“保留配置并补充提示”的分支策略
- [x] 3.3 收敛分析问数、企业微信 AI 理解、合同审核等统一执行门面的缺省推理等级行为，避免各链路继续使用不一致的隐式默认值

## 4. 回归验证

- [x] 4.1 补充后端测试，覆盖新建、编辑、复制、环境默认档案和环境兜底场景下的推理等级默认值
- [x] 4.2 补充前端测试，覆盖治理抽屉的默认回显、修改保存和列表展示
- [x] 4.3 补充 Provider / 运行时相关测试，验证 Codex 消费路径与 Claude 能力分支行为符合实现约束

## 5. 文档同步

- [x] 5.1 更新 AI 模型治理技术文档与 README 中的 AI 配置说明，补充“推理等级可编辑”和“默认最低等级优先速度”的规则
- [x] 5.2 更新 `specs/001-crm-intelligent-analytics/quickstart.md` 等操作文档，说明治理页新增推理等级字段与默认值策略
