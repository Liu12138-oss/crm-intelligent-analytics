## 1. 后端登录与绑定链路修正

- [x] 1.1 调整企业微信 Web 扫码登录解析逻辑，优先复用 CRM 原生 `wx_user_maps -> wx_users` 映射，已同步用户直接建立 Web 会话
- [x] 1.2 调整扫码未命中后的手工绑定流程，使用户名密码认证成功后将正式绑定写回 CRM 原生映射表，并仅保留短时待绑定令牌
- [x] 1.3 收口 `SessionAuthGuard` 与认证 Cookie 逻辑，移除正式运行链路对裸用户标识的降级放行

## 2. 前端会话承载收口

- [x] 2.1 移除前端对 `sessionId` 的本地持久化和请求头透传，统一改为基于 Cookie 的会话水合
- [x] 2.2 调整登录页、路由守卫和登出/失效处理，使其与 Cookie-only 登录态保持一致

## 3. 测试与回归

- [x] 3.1 补充后端测试，覆盖已同步用户扫码直登、手工绑定写回 CRM 原生映射以及裸用户标识访问受保护接口被拒绝
- [x] 3.2 补充或更新前端测试，覆盖登录页与路由在 Cookie-only 模式下的关键行为
- [x] 3.3 运行相关测试并回填任务状态
