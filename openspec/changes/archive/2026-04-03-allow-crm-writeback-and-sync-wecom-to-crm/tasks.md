## 1. 边界与配置调整

- [x] 1.1 更新 `README.md`、`AGENTS.md` 与 `specs/001-crm-intelligent-analytics/` 中“CRM 全局只读”相关表述，改为“链路级读写边界”。
- [x] 1.2 更新运行时配置与说明文档，明确当前项目复用同一套企业微信凭证同时承担网页登录与通讯录同步。
- [x] 1.3 梳理后端模块命名与文案，避免继续把企业微信目录同步描述为“仅应用侧镜像”方案。

## 2. 企业微信到 CRM 原生表同步

- [x] 2.1 改造企业微信目录同步服务，主链路对齐 `gettoken -> department/simplelist -> user/list`。
- [x] 2.2 为 CRM 原生 `wx_users` 增加受控 Upsert 写入能力，按 `(wx_organization_id, userid)` 幂等更新。
- [x] 2.3 视首版需要补齐 `wx_departments`、`wx_user_department_maps` 或明确推迟到后续任务。
- [x] 2.4 调整同步运行状态与执行摘要，统计 `wx_users` 写入结果而不是仅统计应用镜像结果。

## 3. CRM 用户自动绑定生成

- [x] 3.1 实现基于手机号唯一匹配 `users.phone` 的自动绑定逻辑，并写入 `wx_user_maps`。
- [x] 3.2 实现基于邮箱唯一匹配 `users.email` 的自动绑定逻辑，并写入 `wx_user_maps`。
- [x] 3.3 实现“缺少手机号邮箱 / 零命中 / 多命中 / 映射冲突”四类结果的保守处理与审计记录。
- [x] 3.4 为指定企业微信 `userid` 增加可观测的绑定失败原因输出，便于排障。

## 4. 机器人认证链路收口

- [x] 4.1 调整机器人认证逻辑，继续以 CRM 原生 `wx_user_maps -> wx_users` 为主链路。
- [x] 4.2 弱化或移除应用侧 `wecom_sync_users` 作为长期认证兜底主来源的逻辑。
- [x] 4.3 更新未绑定提示口径，使其与 CRM 原生映射同步后的真实排障路径一致。

## 5. 运维入口、测试与文档

- [x] 5.1 改造一键同步脚本与内部同步接口，确保其触发的是“企业微信到 CRM 原生映射”同步。
- [x] 5.2 增加或更新集成测试，覆盖 `wx_users` 写入、`wx_user_maps` 自动生成、零命中、多命中与机器人认证成功场景。
- [x] 5.3 更新企业微信机器人文档、目录同步运维说明和 quickstart，统一为 CRM 原生 `wx_*` 同步与认证方案。
