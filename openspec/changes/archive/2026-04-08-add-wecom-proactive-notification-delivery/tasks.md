## 1. 通知底座与配置

- [x] 1.1 在后端定义统一的主动通知任务模型、渠道枚举、接收人快照、发送结果和幂等键结构。
- [x] 1.2 扩展运行时配置，支持通知通道优先读取专用 `WECOM_NOTIFY_*` 配置，未配置时回落到当前已提供的企业微信自建应用配置，并增加“真实消息通知开关”。
- [x] 1.3 为主动通知补充独立的审计事件、发送记录和失败原因口径，避免继续混用机器人会话下发记录。

## 2. 企业微信发送通道

- [x] 2.1 实现企业微信自建应用消息发送适配器，支持按成员定向发送正式通知，并为后续按部门、按标签扩展保留统一接口，同时记录无效接收人和未授权接收人结果。
- [x] 2.2 扩展智能机器人主动推送适配器，支持面向单聊用户与既有群聊会话的主动消息下发。
- [x] 2.3 实现通知通道路由策略：固定周期类与管理汇总类默认走自建应用，机器人强相关异步回推走智能机器人主动推送，并把“机器人虽可单发但正式通知仍优先自建应用”的规则固化到策略层。
- [x] 2.4 为测试联调补充测试收件人覆盖能力，真实发送开关关闭时统一改投 `王亮2（WangLiang02）` 或等效测试模式。

## 3. 接收人解析与业务接入

- [x] 3.1 抽象正式通知接收人解析接口，支持后续业务提案按组织、负责人、标签或会话上下文注入接收人规则。
- [x] 3.2 在发送前补做接收人映射和当前权限校验，阻断无映射、权限失效或越权场景。
- [x] 3.3 为业务提案提供可复用的通知投递入口，避免业务模块直接操作自建应用或机器人适配器。

## 4. 稳定性与验证

- [x] 4.1 为主动通知实现有限重试、重复投递幂等保护和最终失败告警口径。
- [x] 4.2 补充单测与集成测试，覆盖自建应用单发、机器人主动推送、测试收件人覆盖、权限阻断和失败重试。
- [x] 4.3 同步更新主规格、快速验证和运维说明，明确主动通知已纳入受控能力范围及其通道选型原则。
