## 1. 接口与类型

- [x] 1.1 梳理现有企业微信同步部门、同步成员、CRM `wx_*` 映射、CRM 部门映射和权限中心 `access-options` 数据来源，确认组织对象选项接口复用路径。
- [x] 1.2 在后端共享类型中新增组织对象选项返回结构，覆盖部门、成员、CRM 用户映射状态、CRM 部门映射状态、不可选原因和最近同步时间。
- [x] 1.3 在治理模块新增组织对象选项查询服务，复用现有企业微信组织事实、CRM 身份映射和 CRM 部门关系，不新增独立人员、部门或权限模型。
- [x] 1.4 在治理控制器新增组织对象选项接口，并复用权限中心治理访问校验。
- [x] 1.5 在前端 `analysis.service` 和类型文件中新增组织对象选项接口方法与类型定义。

## 2. 统一组织架构对象选择组件

- [x] 2.1 新增 `WecomOrgSubjectPicker.vue` 共享组件，支持弹窗或抽屉打开、已选摘要展示、确认和取消。
- [x] 2.2 实现企业微信部门树展示、部门展开、部门下人员列表、部门选择状态和已选对象列表。
- [x] 2.3 实现单选与多选模式，保证单选只保留最后一个对象，多选保留全部可选对象。
- [x] 2.4 实现 `subjectTypes` 配置，支持只选人员、只选部门和人员/部门混合选择。
- [x] 2.5 实现部门节点双模式：选择部门本身，以及批量选择当前部门及子部门下所有当前场景可选人员。
- [x] 2.6 实现人员与部门搜索，支持按姓名、企业微信 userid、CRM 用户名、部门名、企业微信部门 ID、CRM 部门名和说明信息过滤，并在选择后自动清空搜索词。
- [x] 2.7 实现 `valueType='crmUserId'`、`valueType='wecomUserId'`、`valueType='crmDepartmentId'` 与 `valueType='wecomDepartmentId'` 输出模式，CRM ID 场景禁用未映射或映射异常对象。
- [x] 2.8 明确组件文案与图标，确保当前模式是“选择人员”“选择部门”还是“选择部门下人员”时用户可感知。
- [x] 2.9 补齐加载中、空状态、失败重试、禁用态、悬浮提示、键盘焦点和清空已选等交互反馈。
- [x] 2.10 按 `DESIGN.md` 与 Element Plus 项目样式补充组件样式，确保移动端和桌面端文本不溢出、不重叠。

## 3. 权限中心接入

- [x] 3.1 替换企业微信准入中的白名单用户和停用用户选择，保存时继续提交 CRM 用户 ID 数组。
- [x] 3.2 替换企业微信准入中的白名单部门选择，保存时继续提交 CRM 部门 ID 数组。
- [x] 3.3 替换超级管理员授权中的已授权人员选择，保存时继续提交 CRM 用户 ID 数组；已授权角色保留现有角色选择。
- [x] 3.4 替换白名单设置中“授权对象类型=用户”时的授权对象选择，角色选择保持现有下拉。
- [x] 3.5 替换白名单设置中的授权部门选择，保存时继续提交 CRM 部门 ID 数组。
- [x] 3.6 替换日报治理新增/编辑团队中的组长选择，使用单选 CRM 用户 ID 模式。
- [x] 3.7 替换日报治理新增/编辑团队中的组员选择，使用多选 CRM 用户 ID 模式，并保留未映射成员回显禁用逻辑。
- [x] 3.8 替换身份映射诊断中的企业微信账号选择，使用企业微信 userid 输出模式。
- [x] 3.9 替换用户权限预览中的 CRM 用户和企业微信账号选择，分别使用 CRM 用户 ID 与企业微信 userid 输出模式。
- [x] 3.10 确认角色权限页没有人员或部门选择，仅保留角色状态筛选和角色权限编辑。
- [x] 3.11 清理权限中心内已不再使用的人员/部门选项拼接、标签删除保护和局部搜索残留处理逻辑。

## 4. 项目规约与后续复用

- [x] 4.1 更新 `AGENTS.md`，明确后续任意页面或新增业务涉及选择人员或部门时，默认复用 `WecomOrgSubjectPicker.vue` 或其项目级封装。
- [x] 4.2 在组件说明或邻近文档中写明允许例外的条件：非企业微信/CRM 组织对象，或组件无法表达且已在 OpenSpec/设计中说明原因。
- [x] 4.3 检查已有其它页面是否存在人员或部门选择入口，若本轮不纳入改造，需在任务说明中记录原因并避免破坏现有行为。
  - 已检查 `frontend/src/pages/audit/AuditEventPage.vue`、`frontend/src/pages/governance/QueryTemplatePage.vue`、`frontend/src/components/management-report/ManagementReportFilters.vue` 等现有入口；审计页和模板页为事件、状态、视图等枚举选择，不属于人员/部门组织对象。本轮未改造经营报表 `DepartmentTreeSelect`，原因是该入口属于经营报表范围筛选的既有专用树控件，不在权限中心改造范围内；后续若重构报表范围或新增企业微信/CRM 人员部门选择，应按 `AGENTS.md` 统一组件规约复用或在 OpenSpec 中说明例外。

## 5. 后端与前端测试

- [x] 5.1 为组织对象选项服务补充后端单元测试，覆盖已映射、未映射、映射异常、已删除人员、已删除部门和权限拒绝。
- [x] 5.2 为组织对象选项接口补充控制器或集成测试，验证未授权用户无法读取组织人员与部门清单。
- [x] 5.3 为 `WecomOrgSubjectPicker.vue` 补充前端单元测试，覆盖人员单选、人员多选、部门选择、部门下人员全选、不可选对象、搜索后清空关键词和输出 ID 类型。
- [x] 5.4 更新权限中心页面单元测试，覆盖企业微信准入、超级管理员授权、白名单设置、日报治理、身份诊断和权限预览的人员/部门选择入口。
- [x] 5.5 补充或更新 e2e 测试，验证搜索选择后关键词清空、部门选择和部门下人员全选后保存 payload 保持原业务字段不变。

## 6. 验收与回归

- [x] 6.1 运行后端相关测试，至少覆盖治理模块、企业微信组织事实、权限预览和超级管理员授权。
- [x] 6.2 运行前端单元测试和权限中心相关 e2e 测试。
- [x] 6.3 启动本地前后端服务，手工验证权限中心所有涉及人员或部门选择的 tab 均可通过组织架构树和搜索完成选择。
- [x] 6.4 手工验证选择搜索结果后搜索框自动清空，已选对象不丢失。
- [x] 6.5 手工验证部门模式只选择部门本身，人员模式下部门全选只加入当前场景可选人员。
- [x] 6.6 手工验证未映射 CRM 的企业微信成员不能保存为 CRM 用户授权主体，未映射 CRM 的企业微信部门不能保存为 CRM 部门授权主体。
- [x] 6.7 检查企业微信准入、超级管理员授权、白名单设置、日报治理、身份映射诊断和用户权限预览的保存或查询结果与改造前业务语义一致。
  - 验收执行记录：`npm test`、`npm run test:unit`、`npm run test:e2e`、`npm run build`（前后端）均已通过；权限中心人员/部门选择验收由 `permission-center-page.spec.ts`、`wecom-org-subject-picker.spec.ts` 和 `element-plus-pages.e2e-spec.ts` 覆盖，其中 e2e 使用本地 Vite 服务验证搜索清空、部门本身选择、部门下人员全选与保存 payload 字段不变。
