## 1. 变更准备与验收口径

- [x] 1.1 确认一级菜单热切换的目标口径：已登录同页签内切换时，路由确认与页面骨架展示必须达到毫秒级
- [x] 1.2 确认首批纳入优化的一级页面：`analysis`、`management-report`、`contract-review`、`governance/*`、`audit`
- [x] 1.3 确认前端与后端都需要增加的性能埋点字段和日志字段

## 2. 前端路由与会话快照改造

- [x] 2.1 调整路由守卫，停止在每次受保护路由切换时强制刷新 `session`
- [x] 2.2 在认证 store 中拆分 `session` 与 `capabilities` 缓存语义，新增短 TTL、时间戳与 Promise 去重
- [x] 2.3 为登录成功、退出登录、401、扫码回流和治理保存补充显式失效逻辑
- [x] 2.4 为前端会话与能力快照补单元测试，覆盖命中缓存、并发去重和失效事件

## 3. 应用壳层与页面恢复策略

- [x] 3.1 为一级菜单页面接入受控 `KeepAlive` 或等价状态保留机制
- [x] 3.2 为应用壳层补“路由即时切换、页面骨架先出”的交互保证
- [x] 3.3 为页面恢复路径补无闪烁、无整页空白的交互回归测试

## 4. 智能分析页分层加载

- [x] 4.1 为分析页 store 增加 `bootstrapped`、最近状态复用和静默刷新机制
- [x] 4.2 将模板列表与最近查询从串行加载改为并行加载
- [x] 4.3 确保再次进入分析页时优先复用最近状态，再后台补齐更新
- [x] 4.4 补分析页菜单往返切换的前端性能与请求数回归

## 5. 经营报表、权限中心和其它重页面懒加载

- [x] 5.1 将经营报表首屏收敛为必要摘要区块，其余专题按需加载
- [x] 5.2 将权限中心首屏收敛为概览与必要候选项，其余分区延迟加载
- [x] 5.3 评估并收敛审计中心、合同审核等页面的首屏最小请求集
- [x] 5.4 为重页面补“先出骨架、后补数据、区块失败可重试”的状态回归

## 6. 后端短时会话解析与能力快照缓存

- [x] 6.1 为会话解析链路增加短时用户快照复用，减少同页签短时间内的重复数据库身份解析
- [x] 6.2 为能力快照增加短时缓存与版本化键设计
- [x] 6.3 为权限矩阵变更、范围变更、401、登录态变化补后端缓存失效逻辑
- [x] 6.4 确保真实数据接口与敏感动作接口继续走后端统一权限校验，不因前端缓存放行
- [x] 6.5 为会话缓存与能力快照缓存补集成测试，覆盖命中、过期和治理保存后失效

## 7. 观测、性能基线与回归

- [x] 7.1 为前端补 `menu_click`、`route_confirmed`、`shell_visible`、`page_data_ready` 分段埋点
- [x] 7.2 为后端补会话解析、能力快照和重页面首屏接口耗时日志
- [x] 7.3 建立“菜单热切换不重复请求 `session` / `capabilities`”的端到端回归
- [x] 7.4 建立“治理保存后下一次导航立即使用新权限结果”的回归
- [x] 7.5 建立首批页面性能采样记录，作为后续性能退化对比基线
