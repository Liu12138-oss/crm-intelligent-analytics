## 1. 日报源数据建模

- [x] 1.1 将本变更的日报口径从“日报草稿 / 人工确认”调整为“按用户 + 业务日期查询 CRM 源数据自动生成摘要”。
- [x] 1.2 明确当天日报源数据的查询范围：`revisit_logs` 跟进记录、`customers` 当天新增记录、`opportunities` 当天新增记录。
- [x] 1.3 明确各类源数据的归属字段、时间字段、去重规则和异常兜底规则，避免同一业务动作被重复计入日报。

## 2. 跟进内容提取与个人日报生成

- [x] 2.1 基于企业微信结构化跟进模板，定义 `跟进内容`、`遇到与协助`、`信息共享`、`拜访计划` 的提取与回退摘要规则。
- [x] 2.2 将跟进记录中的更新人姓名、对象类型、对象名称一并纳入个人日报摘要生成逻辑。
- [x] 2.3 将当天新增客户、当天新增商机合并进“客户 / 商机变化”维度，形成最终个人日报摘要。

## 3. 22点处理与团队汇总调度

- [x] 3.1 将 `22:00` 规则改为双分支：有日报源数据则自动总结个人日报并发本人确认，无日报源数据则仅提醒销售本人补录 CRM 数据。
- [x] 3.2 为 `22:00` 个人确认消息与无数据催报消息定义友好、亲切、鼓励式文案规则，避免生硬催促。
- [x] 3.3 将次日 `08:00` 团队汇总规则改为“按负责人聚合成员个人日报摘要 + 无源数据名单”，不再以旧的确认数作为主口径。
- [x] 3.4 保留销售负责人映射规则，并为售前映射缺失时的阻断与告警留出实现位置。

## 4. 通知底座集成

- [x] 4.1 将日报提醒与团队汇总统一接入 `ProactiveNotificationService`，固定复用企业微信机器人通道。
- [x] 4.2 移除本变更中“日报模块写死测试接收人”的假设，改为复用 `WECOM_NOTIFY_REAL_MESSAGE_ENABLED` 与 `WECOM_NOTIFY_TEST_USER_ID` 配置。
- [x] 4.3 明确日报通知的标签、幂等键、审计记录和失败重试都由主动通知底座统一承接。

## 5. 验证与文档同步

- [x] 5.1 补充单测与集成测试，覆盖结构化跟进提取、当天新增客户 / 商机纳入日报、22:00 自动摘要确认、22:00 无源数据友好催报、08:00 团队汇总、配置驱动测试改投。
- [x] 5.2 复核与同步日报相关快速验证、运维说明和主规格描述，去掉“日报草稿未确认”类旧口径。
- [x] 5.3 补充一组固定回归样例，确保后续继续优化跟进模板或新增创建能力时，不会破坏日报自动汇总口径。
