## 1. 策略模型与迁移

- [x] 1.1 设计应用超级管理员策略类型，支持 `USER` 和 `ROLE` 主体、状态、更新人、更新时间和中文变更原因。
- [x] 1.2 新增或升级策略仓储，读取时兼容历史 `analysisScopePolicy.fullAccessUserIds`。
- [x] 1.3 编写旧“全量查询授权”到应用超级管理员授权的迁移逻辑，保存新策略后不再写旧结构。
- [x] 1.4 为策略读取、保存、禁用、恢复和旧数据兼容补充单元测试。

## 2. 统一权限决策

- [x] 2.1 在统一权限决策服务中接入应用超级管理员策略，命中后返回权限目录中的全部菜单和全部动作。
- [x] 2.2 补齐 `webConsoleEnabled`、`wecomBotEligible`、`exportAllowed`、模板治理、合同上传、合同跨看、合同跨下载、SQL 敏感明细查看等高风险能力的超级管理员覆盖。
- [x] 2.3 确保通道关闭、登录失效、企业微信签名和来源校验不被超级管理员授权绕过。
- [x] 2.4 更新权限预览和当前能力快照，展示应用超级管理员命中状态、全部菜单、全部动作和中文说明。
- [x] 2.5 为普通用户、用户级超级管理员、角色级超级管理员、通道关闭和新增权限点自动纳入补充测试。

## 3. 统一数据范围

- [x] 3.1 在统一范围解析中接入应用超级管理员策略，并输出明确的全量范围标记和中文范围说明。
- [x] 3.2 扩展 `ScopeSnapshot` 类型，区分应用超级管理员全量范围和普通空范围，避免空数组被解释成无结果。
- [x] 3.3 确保普通用户继续按企业微信组织事实、默认负责人范围和数据范围白名单解析，不被其他超级管理员授权影响。
- [x] 3.4 为范围解析、审计快照和普通用户不扩权补充单元测试。

## 4. 业务模块接入

- [x] 4.1 整改经营报表上下文，停止直接使用 `user.ownerIds` 作为最终查询范围，改为消费统一范围快照。
- [x] 4.2 整改经营报表真实 SQL 构造，应用超级管理员全量范围下不得生成 `__none__` 这类无结果占位条件。
- [x] 4.3 整改合同审批 CRM 待审批列表、详情和快照访问，应用超级管理员可查看授权组织内非本人合同。
- [x] 4.4 整改分析问数和查询模板执行，停止直接消费旧分析全量名单，统一使用应用超级管理员范围结果。
- [x] 4.5 整改最近查询重跑、历史结果补查和导出补查，确保超级管理员全量范围与普通范围都实时重算。
- [x] 4.6 为经营报表、合同审批、模板执行、最近查询重跑和导出补查补充集成测试。

## 5. 权限中心前端

- [x] 5.1 将权限中心“全量查询授权”升级为“超级管理员授权”，支持用户和角色两类授权主体。
- [x] 5.2 增加中文高风险提示，说明该授权开放全部功能、全部操作和全量数据范围。
- [x] 5.3 保存授权时要求填写中文变更原因，并覆盖保存中、成功、失败和重试反馈。
- [x] 5.4 更新权限预览展示，显示是否命中应用超级管理员、命中来源和全量范围说明。
- [x] 5.5 补充权限中心单元测试和必要的端到端测试。

## 6. 审计与文档

- [x] 6.1 为应用超级管理员授权变更、权限预览、范围解析和高风险操作命中补充审计事件。
- [x] 6.2 确保审计事件记录操作者、授权主体、主体类型、变更前后摘要、命中来源和变更原因。
- [x] 6.3 检查并更新 README、主规格或权限治理相关文档中关于“全量查询授权”的描述。
- [x] 6.4 清理或标记废弃旧 `AnalysisScopePolicy` 命名，避免后续新功能继续接入旧概念。

## 7. 验证与发布

- [x] 7.1 运行后端权限、范围、经营报表、合同审批、模板执行和导出相关测试。
- [x] 7.2 运行前端权限中心、导航、权限预览和经营报表相关测试。
- [x] 7.3 运行后端构建和前端构建，确认类型与打包通过。
- [x] 7.4 准备发布核对清单，包含旧全量名单迁移结果、超级管理员名单确认、回滚方式和生产权限预览验证步骤。
